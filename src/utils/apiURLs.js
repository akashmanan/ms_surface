import {API_URL} from '@env';

const USER_LOGIN = `${API_URL}login`
const INSPECTION_LISTING = `${API_URL}inspections`
const REGISTER_TOKEN = `${API_URL}register_token`

const Config = {
    API_URL,
    USER_LOGIN,
    INSPECTION_LISTING,
    REGISTER_TOKEN
}

export default Config